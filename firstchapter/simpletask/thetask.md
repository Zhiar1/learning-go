## Take two string values as input from the user, use a function to add them up, convert them to float and check for errors, and if no errors return the result.

```go
package main

import (
	"bufio"
	"fmt"
	"os"
	"strconv"
	"strings"
)

func main(){

}
```